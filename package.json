{
  "name": "backend-takehome-test",
  "version": "0.1.0",
  "private": true,
  "description": "Sano genetic data parser and API (take-home)",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
